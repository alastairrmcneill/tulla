import { ScreenPlaceholder } from '@/components/screen-placeholder';

// Screen 8 (product spec §5) — coach-path only, dynamic headline from the
// quiz's challenge answer (4.10).
export default function OnboardingPaywallScreen() {
  return <ScreenPlaceholder title="Start Your Trial" />;
}
