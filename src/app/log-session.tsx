import { ScreenPlaceholder } from '@/components/screen-placeholder';

// Log a Session / RPE sheet (screen 5) — reachable from a persistent FAB on
// both Today and History (2.8), presented as a modal (see root _layout.tsx).
export default function LogSessionScreen() {
  return <ScreenPlaceholder title="Log a Session" />;
}
